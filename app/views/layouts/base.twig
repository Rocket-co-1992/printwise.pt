<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}{{ app_name }}{% endblock %}</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.0/font/bootstrap-icons.css">
    <!-- Custom CSS -->
    <link rel="stylesheet" href="{{ app_url }}/css/style.css">
    <!-- Favicon -->
    <link rel="icon" type="image/png" href="{{ app_url }}/img/favicon.png">
    
    {% block head_extras %}{% endblock %}
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm">
        <div class="container">
            <a class="navbar-brand" href="{{ app_url }}">
                <img src="{{ app_url }}/img/logo.png" alt="{{ app_name }}" height="40">
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link {% if current_page == 'home' %}active{% endif %}" href="{{ app_url }}">Início</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if current_page == 'about' %}active{% endif %}" href="{{ app_url }}/sobre">Sobre</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if current_page == 'services' %}active{% endif %}" href="{{ app_url }}/servicos">Serviços</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if current_page == 'contact' %}active{% endif %}" href="{{ app_url }}/contato">Contato</a>
                    </li>
                    <li class="nav-item ms-2">
                        <a class="btn btn-primary" href="{{ app_url }}/solicitar-orcamento">Solicitar Orçamento</a>
                    </li>
                    {% if is_logged_in %}
                    <li class="nav-item dropdown ms-2">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                            data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="bi bi-person-circle me-1"></i> {{ user.name }}
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item" href="{{ app_url }}/admin"><i class="bi bi-speedometer2 me-2"></i>Painel</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <form action="{{ app_url }}/logout" method="post" class="d-inline">
                                    <button type="submit" class="dropdown-item text-danger">
                                        <i class="bi bi-box-arrow-right me-2"></i>Sair
                                    </button>
                                </form>
                            </li>
                        </ul>
                    </li>
                    {% else %}
                    <li class="nav-item ms-2">
                        <a class="btn btn-outline-secondary" href="{{ app_url }}/login">
                            <i class="bi bi-lock me-1"></i> Área do Cliente
                        </a>
                    </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main>
        {% block content %}{% endblock %}
    </main>

    <!-- Footer -->
    <footer class="footer mt-5 py-5 bg-dark text-white">
        <div class="container">
            <div class="row">
                <div class="col-md-4 mb-4 mb-md-0">
                    <img src="{{ app_url }}/img/logo-white.png" alt="{{ app_name }}" height="40" class="mb-3">
                    <p class="mb-3">Soluções de impressão profissional para o seu negócio. Qualidade, eficiência e pontualidade.</p>
                    <div class="social-links">
                        <a href="#" class="text-white me-3"><i class="bi bi-facebook"></i></a>
                        <a href="#" class="text-white me-3"><i class="bi bi-instagram"></i></a>
                        <a href="#" class="text-white me-3"><i class="bi bi-linkedin"></i></a>
                        <a href="#" class="text-white"><i class="bi bi-whatsapp"></i></a>
                    </div>
                </div>
                <div class="col-md-4 mb-4 mb-md-0">
                    <h5 class="mb-3">Links Úteis</h5>
                    <ul class="list-unstyled">
                        <li class="mb-2"><a href="{{ app_url }}" class="text-decoration-none text-white-50">Início</a></li>
                        <li class="mb-2"><a href="{{ app_url }}/sobre" class="text-decoration-none text-white-50">Sobre</a></li>
                        <li class="mb-2"><a href="{{ app_url }}/servicos" class="text-decoration-none text-white-50">Serviços</a></li>
                        <li class="mb-2"><a href="{{ app_url }}/contato" class="text-decoration-none text-white-50">Contato</a></li>
                        <li><a href="{{ app_url }}/politica-privacidade" class="text-decoration-none text-white-50">Política de Privacidade</a></li>
                    </ul>
                </div>
                <div class="col-md-4">
                    <h5 class="mb-3">Contato</h5>
                    <p class="mb-2"><i class="bi bi-geo-alt me-2"></i> Rua Principal, 123 - Centro<br>Cidade - Estado, CEP 12345-678</p>
                    <p class="mb-2"><i class="bi bi-telephone me-2"></i> +351 123 456 789</p>
                    <p class="mb-2"><i class="bi bi-envelope me-2"></i> contato@printwise.pt</p>
                    <p><i class="bi bi-clock me-2"></i> Seg-Sex: 9:00 - 18:00</p>
                </div>
            </div>
            <hr class="my-4 border-secondary">
            <div class="row">
                <div class="col-md-6 mb-3 mb-md-0">
                    <p class="mb-0 text-white-50">&copy; {{ "now"|date("Y") }} {{ app_name }}. Todos os direitos reservados.</p>
                </div>
                <div class="col-md-6 text-md-end">
                    <p class="mb-0 text-white-50">Desenvolvido por <a href="#" class="text-decoration-none text-white">YourCompany</a></p>
                </div>
            </div>
        </div>
    </footer>

    <!-- Bootstrap JS Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    
    {% block scripts %}{% endblock %}
</body>
</html>